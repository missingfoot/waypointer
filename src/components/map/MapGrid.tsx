import React from 'react';

interface MapGridProps {
  scale: number;
}

export const MapGrid: React.FC<MapGridProps> = ({ scale }) => {
  return null; // Remove the visible grid while keeping the component for future use
};